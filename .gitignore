# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.claude

# Subagent-driven-development scratch (per-plan ledgers, briefs, review packages)
.superpowers/
tsconfig.tsbuildinfo
.worktrees/

# Staged Claude config payload (rebuilt by scripts/stage-claude-config.sh)
docker/claude-config/

# Playwright browser-test artifacts (e2e/*.smoke.mjs), if ever produced
test-results/
playwright-report/
docker-compose.override.yml

# oh-my-agent runtime (local artifacts — do not commit)
.agents/oma-config.local.cue
.agents/oma-config.local.yaml
.antigravitycli/
.agents/results/
.agents/state/
.agents/backup/
.migration-backup/
.qwen/tmp/

# oh-my-agent vendor projections — generated from the .agents/ SSOT.
# Regenerate with: bunx oh-my-agent@latest link
.claude/
.codex/
.cursor/
.grok/
.qwen/
.zcode/
.serena/
.mcp.json
.github/mcp.json
.github/prompts/
.github/skills/
AGENTS.md

# Agent scratch (plan gate B2 state, hook markers) — never shared
.agent/
.agents/plan.md
.agents/source-edited

# Agent orchestration journals (dag runs, child sessions)
.omo/

# Coordination artifacts from a workflow run, same class as .agents/results and .agents/state.
.agents/memory/
